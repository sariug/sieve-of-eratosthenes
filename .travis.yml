sudo: false
language: cpp
cache:
  directories:
  - mpich
before_install:
  - sh ./get_omp.sh
script:
  - mkdir build && cd build
  - cmake ..
  - make
  - ./mpich/bin/mpirun -np 4 ./sieve_of_eratosthenes


